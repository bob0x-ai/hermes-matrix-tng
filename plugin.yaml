name: matrix-platform-tng
label: Matrix
kind: platform
version: 0.1.0-tng
description: >
  Hermes Matrix TNG adapter with instance-owned profile paths for safe
  multi-profile gateway operation. It is tested but not deployed by default.
author: Local Hermes Operations
requires_env:
  - name: MATRIX_HOMESERVER
    description: "Matrix homeserver URL"
    prompt: "Matrix homeserver URL"
    password: false
  - name: MATRIX_ACCESS_TOKEN
    description: "Matrix access token (or use MATRIX_PASSWORD)"
    prompt: "Matrix access token"
    password: true
optional_env:
  - name: MATRIX_PASSWORD
    description: "Matrix account password"
    prompt: "Matrix password"
    password: true
